apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: homolfront
spec:
  type: ExternalName
  externalName: backend-service.homolback.svc.cluster.local
  ports:
    - port: 8080
      targetPort: 8080
